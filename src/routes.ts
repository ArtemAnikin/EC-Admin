/**
 * Route path constants for the Easy Cars Admin Panel.
 * Use these when defining navigation and route matching.
 */
export enum ROUTES {
  DASHBOARD = '/',
  LOGIN = '/login',
  SETTINGS = '/settings',
  DELIVERY = '/delivery',
}
